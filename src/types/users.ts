export interface Users {
    id: number,
    username: string,
    password: string,
}
export interface InfoUsers {
    id: number;
    firstName?: string;
    lastName?: string;
    email?: string;
    phoneNumber?: string;
    idUser: number;
}